apiVersion: v1
kind: Service
metadata:
  name: sync-server
spec:
  type: NodePort
  selector:
    type: sync-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080