version: '3.4'
services:
  natssync-server-arm:
    build:
      context: .
      dockerfile: ./DockerfileArm
      target: natssync-server-arm
  natssync-client-arm:
    build:
      context: .
      dockerfile: ./DockerfileArm
      target: natssync-client-arm
  echo-proxylet-arm:
    build:
      context: .
      dockerfile: ./DockerfileArm
      target: echo-proxylet-arm
  simple-reg-auth-arm:
    build:
      context: .
      dockerfile: ./DockerfileArm
      target: simple-reg-auth-arm